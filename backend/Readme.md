# backend/

Express + TypeScript API, Prisma/PostgreSQL (or in-memory fallback), Socket.IO, mock adapters.

**Phase 1:** folder preserved; no server code yet.

First runnable slice (Phase 2): `GET /api/health` and `GET /api/incidents` from `mock-data/hackwell-city.json`. LLM must not accept dispatch.
