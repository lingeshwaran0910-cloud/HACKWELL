# shared/

Canonical TypeScript contracts for SafeCity AI.

- `types.ts` — enums, entities, Socket.IO event names

Backend, frontend, intelligence, and optimization must import these types instead of duplicating shapes.
