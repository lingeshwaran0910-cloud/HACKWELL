# frontend/

Operator console for SafeCity AI (React + Vite + TypeScript + Tailwind + Leaflet + Recharts + Socket.IO client).

**Phase 1:** folder preserved; no application code yet.

Implementation starts in Phase 2: map of Hackwell zones and seed incidents. Do not invent API fields — use `shared/types.ts` and `docs/API-CONTRACT.md`.
